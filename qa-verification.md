# 視覺與執行驗證紀錄

## 2026-08-19

| 驗證面向 | 結果 | 觀察重點 |
| --- | --- | --- |
| 桌面工作區 | 通過 | 1440px 寬度下，左側工具列、中央內嵌預覽、右側屬性面板與下方三軌時間軸均可見且無重疊。 |
| 手機工作區 | 通過 | 390px 寬度下，工具列維持可觸控的垂直排列，預覽、工具面板與時間軸依序排列，內容不會橫向截斷；最新字幕與輸出動作更新後再次確認正常。 |
| 型別與單元測試 | 通過 | `pnpm check` 成功；Vitest 共 3 項測試成功，覆蓋 SRT 時戳與字幕匯出。 |
| 生產影音相依 | 通過 | Dockerfile 安裝 FFmpeg、fontconfig 與 Noto CJK 字型，支援伺服器端合成及字幕渲染。 |
| 最新執行期紀錄 | 通過 | 以 21:41 更新為起點的日誌區間檢查結果為伺服器錯誤 0、瀏覽器錯誤 0；歷史紀錄中保留一次 21:37 的舊 HMR 失敗訊息，後續型別檢查、重載與截圖均已成功。 |
| 預覽媒體串流 | 通過 | 實際上傳的 MP4 以 `Range: bytes=0-1023` 請求後回傳 `206 Partial Content`、`content-type: video/mp4` 與 `content-range`。檔案經探測為 H.264 High / AAC-LC / yuv420p，可由主流瀏覽器原生解碼。 |
| 預覽文字拖曳 | 通過 | 桌面與 390px 行動尺寸均能正常呈現預覽文字、播放控制與工具面板；文字圖層改用Pointer事件與 `touch-none`，可同時支援滑鼠拖曳及觸控拖曳，座標會限制為預覽畫面的 0–100% 範圍。Pointer控制器另以 2 項測試覆蓋down／move／up的捕捉、座標更新及釋放流程。 |
| 輸出儲存鍵值 | 通過 | 影片輸出檔名及S3鍵值均改為ASCII安全格式。中文專案名稱會使用 `clipflow` 後備名稱；含重音字元與符號的名稱會正規化為ASCII slug，避免預先簽名服務拒絕非ASCII檔案路徑。 |
| AI字幕預覽與輸出 | 通過 | 桌面與390px行動尺寸均能顯示辨識字幕及既有播放工作區。字幕以Pointer事件支援滑鼠或觸控拖曳，雙擊可在預覽區直接改字；選取字幕時會顯示浮動起訖時間編輯器。Whisper時間戳會依選取片段的裁切與時間軸位置對齊，渲染時以相同百分比座標寫入ASS `\pos`，燒錄至輸出MP4。 |
| API服務錯誤處理 | 通過 | tRPC傳輸層與影片／字型上傳皆先驗證回應是否為可解析JSON；當閘道回傳純文字 `Service Unavailable` 或非JSON內容時，改以Toast顯示可重試的中文服務提示，不再傳遞 `Unexpected token` 解析例外。502、503、504查詢採最多兩次指數退避重試；字幕辨識與影片輸出等有副作用的操作保留使用者確認，由工具列直接顯示手動重試按鈕。標籤條件與重送原始動作另由2項單元測試覆蓋；有效JSON業務錯誤仍保留原始tRPC錯誤流程。 |

> 最新版本在空白專案狀態下的桌面與手機預覽均正常。預覽來源後備與播放錯誤提示已由 3 項單元測試覆蓋；實際 Whisper 辨識及 FFmpeg 渲染需要使用者登入並上傳可處理的影片素材後執行。
